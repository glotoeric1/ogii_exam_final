import 'package:flutter/material.dart';

class AutreDemande extends StatelessWidget {
  const AutreDemande({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Column(
        children: [
          Text("Hello Autre"),
        ],
      ),
    );
  }
}
